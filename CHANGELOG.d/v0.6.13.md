# v0.6.13

### Fixed

- Reviewed Automation A1 before starting A2.
- Disabled `Run once` while periodic automation is Running or Paused so an active scheduled run cannot receive an unexpected manual extra capture.
- Updated the lazy `dpo4000_utils.gui_qt.QtScopeWindow` export and the desktop runner to the reviewed Automation launch layer.
- Extended GUI driver-boundary tests to cover Automation modules and assert that periodic image capture uses the public `scope.save_image_path(path)` API without raw SCPI, `query()`, `write()`, `HARDCOPY`, or `CURVE?` operations.

### Version

- Package version bumped to `0.6.13`.

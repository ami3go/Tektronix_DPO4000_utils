# v0.6.70 — Ruff CI policy stabilization

- Pin Ruff CI to explicit release-blocking Python correctness rules (`E4`, `E7`, `E9`, `F`) so Ruff upgrades cannot silently expand the enforced rule set.
- Baseline only the exact files that contain pre-existing unused-import debt; new `F401` violations elsewhere remain CI failures.
- Keep the legacy `tektronix_utils.py` compatibility re-export explicitly exempt from `F403`.
- Preserve the broader lint findings as cleanup debt rather than suppressing them with repository-wide ignores.

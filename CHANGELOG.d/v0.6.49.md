# v0.6.49

### Fixed

- Reviewed and hardened Logger **L10 Automatic Reconnect**.
- Fixed double-counting of the record that reaches the consecutive transport-failure stop threshold.
- When reconnect retries are exhausted and Logger stops, successfully closed segments are registered with retention before the run enters Failed state.
- Primary transport failures remain authoritative if output close or retention finalization also reports an error.

### Version

- Package version bumped to `0.6.49`.

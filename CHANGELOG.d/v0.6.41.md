# v0.6.41

### Fixed after L4 review

- DPO4LOG waveform snapshots now restore multi-byte raw samples correctly across little-endian/big-endian hosts.
- Snapshot reconstruction validates the decoded sample count against frame metadata.
- CSV conversion paths now use the same endian-aware snapshot decoder.

### Version

- Package version bumped to `0.6.41`.

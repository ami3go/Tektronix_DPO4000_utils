# v0.6.72

- Update the PySide6 runtime smoke test to validate the hardened shared `scope_session()` worker boundary instead of requiring the removed direct `DPO4054` construction path.
- Assert that the stable Qt layer does not restore direct instrument construction or direct timeout mutation.
- Preserve the v0.6.71 Ruff correctness gate and the existing Python 3.10–3.13 test matrix.

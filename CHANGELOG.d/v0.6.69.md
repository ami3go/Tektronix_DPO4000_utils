# v0.6.69 — Remove remaining Qt transport debt

- Migrated legacy channel and MATH configuration handlers in `enhanced_window.py` to the public `ChannelConfig`/`MathConfig` driver APIs and removed GUI-owned SCPI query maps/helpers.
- Replaced direct underlying-session setup in `stable_window.py` with the public `scope_session()` context manager.
- Replaced the last direct `scope.scope.query("*IDN?")` GUI call with `query_identity()`.
- Removed the GUI-boundary debt baseline: the permanent AST regression now requires zero raw VISA/SCPI ownership across every current and future `dpo4000_utils/gui_qt` module.

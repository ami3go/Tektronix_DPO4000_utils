# v0.6.39

### Added

- Implemented Logger backlog item **L3 CSV waveform output**.
- Waveform acquisitions now append to one run CSV segment instead of creating one file per record.
- Each complete record block includes sequence/timestamp, aligned time axis and selected CH/MATH engineering values and is flushed before returning.

### Version

- Package version bumped to `0.6.39`.

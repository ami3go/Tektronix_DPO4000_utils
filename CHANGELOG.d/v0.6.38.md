# v0.6.38

### Added

- Implemented Logger backlog item **L2 MATH waveform logger**.
- Added MATH as an independent selectable waveform source alongside CH1..CH4.
- MATH uses the same public `read_waveform()` path and compact Logger record representation as analog channels.

### Version

- Package version bumped to `0.6.38`.

# v0.6.34

### Fixed after A11 review

- Recovery retry/reconnect statistics are reset at the start of each new Automation run/Run once operation.
- Successful manual or other non-replay-safe scope actions no longer erase an Automation consecutive-transport-failure streak.
- This keeps the configured stop-after-consecutive-failures policy scoped to the unattended Automation workload rather than unrelated GUI traffic.

### Version

- Package version bumped to `0.6.34`.

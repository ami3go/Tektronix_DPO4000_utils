# v0.6.26

### Fixed after A7 review

- Burst count validation now rejects fractional numeric values instead of silently truncating them.
- Burst Single-acquisition configuration now requires a real boolean value so future JSON/profile input cannot truth-coerce strings such as `"false"`.
- Added regression coverage for strict burst configuration validation.

### Version

- Package version bumped to `0.6.26`.

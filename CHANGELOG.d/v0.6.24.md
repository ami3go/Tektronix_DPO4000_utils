# v0.6.24

### Fixed after A6 verification

- Corrected the stopped-run stale-completion guard so the base conditional handler cannot invoke Stop a second time after a user already stopped an in-flight poll.
- The reviewed conditional layer now marks discarded completions explicitly and consumes the follow-up base Stop call without changing the stopped run generation again.
- Added static regression coverage for the discard flag and stopped-state guard.

### Version

- Package version bumped to `0.6.24`.

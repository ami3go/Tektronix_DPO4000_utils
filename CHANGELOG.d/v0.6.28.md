# v0.6.28

### Fixed after A8 review

- The elapsed-duration watchdog now stops immediately when an automation mode completes naturally.
- Count-only run limits no longer run the 250 ms duration watchdog unnecessarily; event hooks enforce count boundaries.
- Added explicit natural-completion cleanup for periodic/logging ticks, trigger cycles, Image+CSV trigger cycles, and Burst events.

### Version

- Package version bumped to `0.6.28`.

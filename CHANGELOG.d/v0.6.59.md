# v0.6.59

Production hardening: fresh Single-acquisition verification.

- Add one shared fresh-Single wait state machine for Automation and Logger.
- Require observing a new active/armed trigger state before accepting `SAVE` as completion.
- Add bounded trigger timeouts so never-triggering scopes cannot block the worker indefinitely.
- Apply the shared wait logic to A2 Image on Trigger, A3 Image+CSV on Trigger, A7 Single Burst, and Mixed Logger capture.
- Reject stale pre-existing `SAVE` state without writing evidence artifacts.
- Add regression coverage for stale-state timeout, cancellation, and verified fresh completion.

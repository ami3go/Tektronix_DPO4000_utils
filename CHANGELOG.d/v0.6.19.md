# v0.6.19

### Fixed after A4 review

- Timed Waveform Logging now clears stale previous-mode file status when a new CSV-only run or Run once starts.
- Replaced the inherited A3 pair-specific Output text with mode-aware output guidance covering Periodic Image, trigger bundles, and Timed Waveform Logging.
- Simplified full-record helper failure bookkeeping and added explicit regression coverage proving transport failures still propagate to the existing session recovery path.

### Version

- Package version bumped to `0.6.19`.

# Archived GUI versions

Historical GUI snapshots are kept here for reference only. The active GUI entry point is:

```text
src/dpo4000_utils/gui/app.py
```

Use `dpo4000-gui` or `python -m dpo4000_utils.gui` for normal operation.
